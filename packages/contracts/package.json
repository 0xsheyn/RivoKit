{
  "name": "@rivokit/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "typecheck": "forge build --sizes",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url $ARC_RPC --broadcast",
    "clean": "forge clean"
  }
}
