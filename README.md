# RivoSDK
Rivo is an inbound money router — a routing infrastructure in Arc where recipients program how incoming money is handled: split it, convert it, and send it to many destinations in one atomic, sub-second transaction. Payroll, payouts, royalties — one API routes them all.
